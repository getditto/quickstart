spring.application.name=quickstart
server.port=8080

# Use user's local app data directory for App-V compatibility
# Falls back to build/ditto-spring-dir for local development
ditto.dir=${LOCALAPPDATA:${user.home}/.local/share}/DittoQuickstart/data
