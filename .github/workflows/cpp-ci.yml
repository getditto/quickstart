name: C++ TUI CI
on:
  push:
    branches: [main]
    paths:
      - 'cpp-tui/**'
  pull_request:
    branches: [main]
    paths:
      - 'cpp-tui/**'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  DITTO_SDK_VERSION: "4.12.0"

jobs:
  build-and-test:
    name: Build and Test C++ TUI
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake clang clang-format build-essential
      
      - name: Create .env file
        run: |
          echo "DITTO_APP_ID=${{ secrets.DITTO_APP_ID }}" > .env
          echo "DITTO_PLAYGROUND_TOKEN=${{ secrets.DITTO_PLAYGROUND_TOKEN }}" >> .env
          echo "DITTO_AUTH_URL=${{ secrets.DITTO_AUTH_URL }}" >> .env
          echo "DITTO_WEBSOCKET_URL=${{ secrets.DITTO_WEBSOCKET_URL }}" >> .env
      
      - name: Insert test document into Ditto Cloud
        run: |
          # Use GitHub run ID to create deterministic document ID
          DOC_ID="cpp_github_test_${GITHUB_RUN_ID}_${GITHUB_RUN_NUMBER}"
          
          # Insert document using curl with correct JSON structure
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
            -H 'Content-type: application/json' \
            -H "Authorization: Bearer ${{ secrets.DITTO_API_KEY }}" \
            -d "{
              \"statement\": \"INSERT INTO tasks DOCUMENTS (:newTask) ON ID CONFLICT DO UPDATE\",
              \"args\": {
                \"newTask\": {
                  \"_id\": \"${DOC_ID}\",
                  \"title\": \"C++ GitHub Test Task ${GITHUB_RUN_ID}\",
                  \"done\": false,
                  \"deleted\": false
                }
              }
            }" \
          "https://${{ secrets.DITTO_API_URL }}/api/v4/store/execute")
          
          # Extract HTTP status code and response body
          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | head -n-1)
          
          # Check if insertion was successful
          if [ "$HTTP_CODE" -eq 200 ] || [ "$HTTP_CODE" -eq 201 ]; then
            echo "✓ Successfully inserted C++ test document with ID: ${DOC_ID}"
            echo "GITHUB_TEST_DOC_ID=${DOC_ID}" >> $GITHUB_ENV
          else
            echo "❌ Failed to insert document. HTTP Status: $HTTP_CODE"
            echo "Response: $BODY"
            exit 1
          fi
      
      - name: Run linter
        working-directory: cpp-tui/taskscpp
        run: |
          echo "🎨 Running clang-format check..."
          make format
          # Check if files were modified
          if ! git diff --exit-code; then
            echo "❌ Code style issues found. Run 'make format' to fix."
            exit 1
          fi
          echo "✅ Code style check passed"
      
      - name: Download and setup Ditto C++ SDK
        working-directory: cpp-tui/taskscpp
        run: |
          echo "📥 Downloading real Ditto C++ SDK v${{ env.DITTO_SDK_VERSION }} for integration test..."
          curl -O https://software.ditto.live/cpp-linux-x86_64/Ditto/${{ env.DITTO_SDK_VERSION }}/dist/Ditto.tar.gz
          tar xvfz Ditto.tar.gz
          
          # Copy SDK files to expected location
          cp Ditto.h sdk/
          cp libditto.a sdk/
          
          echo "✅ Real Ditto C++ SDK installed"
      
      - name: Build application
        working-directory: cpp-tui/taskscpp
        run: |
          echo "🔧 Generating env.h..."
          awk -f scripts/generate_env.awk ../../.env > src/env.h
          
          echo "🔨 Building C++ TUI application with REAL Ditto SDK..."
          make build
          
          echo "✅ C++ TUI app built with real Ditto SDK"
      
      - name: Run integration tests with REAL Ditto SDK
        working-directory: cpp-tui/taskscpp
        env:
          GITHUB_TEST_DOC_ID: ${{ env.GITHUB_TEST_DOC_ID }}
          GITHUB_RUN_ID: ${{ github.run_id }}
          GITHUB_RUN_NUMBER: ${{ github.run_number }}
        run: |
          echo "🧪 Running REAL C++ integration tests with Ditto Cloud..."
          echo "📝 Test document ID: ${GITHUB_TEST_DOC_ID}"
          echo "🌐 This test will connect to REAL Ditto Cloud and verify sync works"
          
          # Build integration test with REAL Ditto SDK
          g++ -std=c++17 -I./src -I./sdk -I./third_party/cxxopts/include \
            tests/integration_test.cpp \
            src/task.cpp src/tasks_peer.cpp src/tasks_log.cpp \
            -L./sdk -lditto -ldl -lrt -pthread \
            -o build/integration_test
          
          echo "🚀 Running integration test with real Ditto SDK..."
          ./build/integration_test
          
          echo "✅ REAL C++ integration tests completed successfully"