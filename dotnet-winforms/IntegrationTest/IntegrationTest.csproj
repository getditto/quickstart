<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Ditto" Version="5.0.3" />
    <PackageReference Include="System.Text.Json" Version="9.0.4" />
  </ItemGroup>

  <ItemGroup>
    <!-- Reference the shared source files from TasksApp -->
    <Compile Include="..\TasksApp\TaskModel.cs" Link="Shared\TaskModel.cs" />
    <Compile Include="..\TasksApp\DittoManager.cs" Link="Shared\DittoManager.cs" />
    <Compile Include="..\TasksApp\TasksRepository.cs" Link="Shared\TasksRepository.cs" />
  </ItemGroup>

  <ItemGroup>
    <!-- Copy .env file to output directory if it exists -->
    <Content Include="../../.env" Condition="Exists('../../.env')">
      <CopyToOutputDirectory>Always</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

</Project>
