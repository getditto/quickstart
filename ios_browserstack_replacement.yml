  browserstack-ios:
    name: BrowserStack iOS Testing  
    runs-on: ubuntu-latest
    needs: build-ios
    if: false # Disabled: KMP project needs proper iOS UI test target for BrowserStack integration
    timeout-minutes: 1
    
    steps:
    - name: iOS BrowserStack Testing Disabled
      run: |
        echo "📱 iOS BrowserStack testing is currently disabled"
        echo ""
        echo "💡 Reason: The KMP iOS project needs proper XCUITest setup"
        echo ""
        echo "🔧 To enable iOS BrowserStack testing:"
        echo "   1. Open kotlin-multiplatform/iosApp/iosApp.xcodeproj in Xcode"
        echo "   2. Add iOS UI Testing Bundle target (File > New > Target > iOS UI Testing Bundle)" 
        echo "   3. Configure the target name as 'iosAppUITests'"
        echo "   4. Add UI test code to verify document sync"
        echo "   5. Share the iosAppUITests scheme (Product > Scheme > Manage Schemes)"
        echo "   6. Commit the new .xcscheme file"
        echo "   7. Change 'if: false' to 'if: needs.build-ios.outputs.ios-build-success == true' in this job"
        echo ""
        echo "📚 Reference implementation: See swift/TasksUITests/TasksUITests.swift"
        echo "✅ Alternative: KMP native tests already run via Gradle on all platforms"