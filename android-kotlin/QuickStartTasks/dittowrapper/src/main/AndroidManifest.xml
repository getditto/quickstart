<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuickStartTasks">

        <!-- Blank MainActivity to allow installation as independent APK -->
        <!-- Main functionality provided through AIDL services -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AIDL Service for Ditto functionality -->
        <service
            android:name=".DittoService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="live.ditto.quickstart.dittowrapper.DittoService" />
            </intent-filter>
        </service>

    </application>

</manifest>